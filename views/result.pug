div
    table.table.table-hover.table-custom.mx-3
        tr
            th(scope='col') 조
            th(scope='col') 총점
            th(scope='col') 순위
        -for( let iNum=0;iNum<teams.length;iNum++)
                
            case rank.final[iNum]
                when 1
                    tr.table-first
                        th(scope='row')= teams[iNum]
                        td= totalPoint[iNum]
                        td= rank.final[iNum]
                default
                    tr
                        th(scope='row')= teams[iNum]
                        td= totalPoint[iNum]
                        td= rank.final[iNum]
        tr
            td
            td
            td#newMem
                    a.btn.btn-outline-main.btn-sm(href='/weekResult?menu=detailed') 상세보기